summary: "Extract contract data via LLM"
description: "Fetches prompt from Langfuse, calls Groq LLM, returns extracted data."
lock: ""
kind: script
schema:
  $schema: "https://json-schema.org/draft/2020-12/schema"
  type: object
  properties:
    pdfText:
      type: string
      description: "Extracted PDF text"
    workflowId:
      type: string
      description: "Workflow UUID"
    verticalSlug:
      type: string
      description: "Vertical identifier"
  required:
    - pdfText
    - workflowId
    - verticalSlug
