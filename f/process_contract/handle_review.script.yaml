summary: "Handle human review decision"
description: "Calls service POST /workflows/:id/review — approve, reject, or correct."
lock: ""
kind: script
schema:
  $schema: "https://json-schema.org/draft/2020-12/schema"
  type: object
  properties:
    workflowId:
      type: string
      description: "Workflow ID"
    action:
      type: string
      enum: ["approve", "reject", "correct"]
      description: "Review action"
    correctedData:
      type: object
      description: "Corrected contract data (required for 'correct' action)"
    notes:
      type: string
      description: "Optional reviewer notes"
  required:
    - workflowId
    - action
