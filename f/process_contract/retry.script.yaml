summary: "Retry failed workflow"
description: "Calls service POST /workflows/:id/retry to resume from the failed step."
lock: ""
kind: script
schema:
  $schema: "https://json-schema.org/draft/2020-12/schema"
  type: object
  properties:
    workflowId:
      type: string
      description: "Workflow ID"
  required:
    - workflowId
