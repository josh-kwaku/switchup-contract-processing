summary: "Compare tariffs (mock)"
description: "Mock tariff comparison step. Transitions to completed."
lock: ""
kind: script
schema:
  $schema: "https://json-schema.org/draft/2020-12/schema"
  type: object
  properties:
    workflowId:
      type: string
      description: "Workflow UUID"
    contractId:
      type: string
      description: "Contract UUID"
  required:
    - workflowId
    - contractId
