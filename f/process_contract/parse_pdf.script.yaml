summary: "Extract text from PDF"
description: "Parses base64 PDF, extracts text content, transitions state."
lock: ""
kind: script
schema:
  $schema: "https://json-schema.org/draft/2020-12/schema"
  type: object
  properties:
    pdfBase64:
      type: string
      description: "Base64-encoded PDF"
    workflowId:
      type: string
      description: "Workflow UUID"
  required:
    - pdfBase64
    - workflowId
